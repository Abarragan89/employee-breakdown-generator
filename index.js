"use strict";

const { managerQuestions } = require('./src/app');

// start questions
managerQuestions();

// save answers (made cards)
// make html page (insert cards)
// put it in the dist directory